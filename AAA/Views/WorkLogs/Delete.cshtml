@model AAA.Models.WorkLog

@{
    ViewData["Title"] = "Удалить запись";
}

<h2>@ViewData["Title"]</h2>

<h4>Вы уверены, что хотите удалить эту запись?</h4>

<dl class="row">
    <dt class="col-sm-2">Сотрудник</dt>
    <dd class="col-sm-10">@Model.Employee?.FullName</dd>

    <dt class="col-sm-2">Начало</dt>
    <dd class="col-sm-10">@Model.StartTime</dd>

    <dt class="col-sm-2">Окончание</dt>
    <dd class="col-sm-10">@Model.EndTime</dd>
</dl>

<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Удалить</button>
    <a asp-action="Index" class="btn btn-secondary">Отмена</a>
</form>
